import React from 'react';

// Stateless function
const ProjectArrows = (props) => {

    return (
        <React.Fragment>
            <i className="material-icons arrow arrow-left">navigate_before</i>
            <i className="material-icons arrow arrow-right">navigate_next</i>
        </React.Fragment>
    );
};

export default ProjectArrows;
